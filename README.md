# BOOK-STORE-API
The Book Store API is a RESTful service that allows users to interact with a digital book store. It provides endpoints for user authentication, book retrieval, and review management. The API is built using Node.js, with Async/Await and Promises for efficient handling of requests.
